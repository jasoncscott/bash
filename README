# =============================================================================
# Symlinks
# =============================================================================
Documents/shell/
~/.bash_logout
~/.bash_profile
~/.gitconfig
~/.rc (pointed to from shell's respective run commands file)
~/.plan
~/.rezconfig.py
~/.rez-version
~/.vim/


# =============================================================================
# Copies
# =============================================================================

~/.forward
~/.pgpkey
~/.plan
~/.project
~/.viminfo (Not currently used)
